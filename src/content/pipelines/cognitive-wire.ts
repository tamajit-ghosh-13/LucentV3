// This TypeScript wire connects the DOM to the Python cognitive ML engine.
// It extracts dense, complex text or distractor elements (ADHD mode) and sends them to the backend.
// It receives plain-language summaries and applies them non-destructively to the webpage.
// It also coordinates hiding sensory clutter based on the AI's classification.
