import { Search, Bell, ChevronDown, LogOut } from 'lucide-react'; import { useLucent } from '../../lib/lucent-state'; import { useNavigate } from 'react-router-dom';
export default function Navbar(){const {user,signOut}=useLucent();const navigate=useNavigate();const leave=()=>{signOut();navigate('/')};const initial=(user?.name||'U').trim().charAt(0).toUpperCase();return <header className="dashboard-navbar"><div className="dashboard-navbar-search"><Search size={18}/><input aria-label="Search dashboard" placeholder="Search your accessibility tools..."/><span className="search-shortcut">⌘ K</span></div><div className="dashboard-navbar-right"><button className="dashboard-icon-button" aria-label="Notifications"><Bell size={19}/><span className="notification-dot"/></button><div className="dashboard-user"><div className="dashboard-user-avatar">{initial}</div><div className="dashboard-user-info"><strong className="dashboard-user-name">{user?.name}</strong><span className="dashboard-user-role">{user?.guest?'Guest session':user?.email}</span></div><ChevronDown size={16}/></div><button className="dashboard-icon-button" title="Log out" aria-label="Log out" onClick={leave}><LogOut size={17}/></button></div></header>}
