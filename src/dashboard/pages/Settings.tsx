import { User, Bell, Moon, Shield, Puzzle, Sun, Eye } from 'lucide-react'; import { useLucent } from '../../lib/lucent-state';
export default function Settings(){const {user,settings,update,theme,setTheme}=useLucent();return <div className="dashboard-page"><div className="dashboard-page-header"><div><div className="dashboard-eyebrow">PREFERENCES</div><h1>Settings</h1><p>Account, extension and display preferences.</p></div></div><div className="settings-container"><section className="settings-section"><div className="settings-section-title"><User size={19}/><div><h2>Profile</h2><p>Your signed-in Lucent identity</p></div></div><div className="settings-fields"><div className="settings-field"><label>Name</label><input value={user?.name||''} disabled/></div><div className="settings-field"><label>Email</label><input value={user?.email||''} disabled/></div></div></section><section className="settings-section"><div className="settings-section-title"><Puzzle size={19}/><div><h2>Extension</h2><p>Controls sync to the Chrome extension when connected.</p></div></div><Toggle icon={<Shield size={18}/>} title="Automatically start with my profile" description="Enable the selected profile when Lucent opens." enabled={settings.enabled} onToggle={()=>update({enabled:!settings.enabled})}/><Toggle icon={<Bell size={18}/>} title="Steady click protection" description="Prevent accidental repeat clicks on interactive controls." enabled={settings.motor.steadyClick} onToggle={()=>update({motor:{...settings.motor,steadyClick:!settings.motor.steadyClick}})}/><Toggle icon={<Eye size={18}/>} title="High-contrast solar theme" description="Enforce high-contrast yellow-on-black color scheme across sites." enabled={settings.visual.highContrast} onToggle={()=>update({visual:{...settings.visual,highContrast:!settings.visual.highContrast}})}/></section><section className="settings-section"><div className="settings-section-title"><Moon size={19}/><div><h2>Appearance</h2><p>Choose a dashboard color theme.</p></div></div><div className="appearance-options"><button className={`appearance-option ${theme==='dark'?'appearance-option-active':''}`} onClick={()=>setTheme('dark')}><Moon size={15}/>Dark</button><button className={`appearance-option ${theme==='light'?'appearance-option-active':''}`} onClick={()=>setTheme('light')}><Sun size={15}/>Light</button></div></section></div></div>}
function Toggle({icon,title,description,enabled,onToggle}:{icon:React.ReactNode;title:string;description:string;enabled:boolean;onToggle:()=>void}){return <div className="setting-toggle-row"><div className="setting-toggle-icon">{icon}</div><div className="setting-toggle-text"><strong>{title}</strong><span>{description}</span></div><button aria-label={`Toggle ${title}`} className={`feature-toggle ${enabled?'feature-toggle-active':''}`} onClick={onToggle}><span/></button></div>}
