# This Python file validates Supabase JWT tokens sent from the Chrome Extension.
# It ensures that only authorized users can trigger expensive ML logic (like Gemini API calls)
# and securely retrieves the user's impairment profile for personalization.
