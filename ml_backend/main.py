# This is the main entry point for the Python ML Backend (e.g., FastAPI / Flask application).
# It serves as the bridge for the Chrome Extension Service Worker to execute AI/ML tasks.
# It mounts the routers for authentication, telemetry logging, and the 3 impairment pipelines.
