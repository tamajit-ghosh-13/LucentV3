# This Python module implements the ML logic for the Motor Impairment Pipeline.
# It processes cursor trajectory telemetry and click metadata sent from the extension.
# It runs the logic to determine if a target is consistently missed (tremor analysis).
# It returns the optimal hit-box expansion parameters back to motor-wire.ts.
