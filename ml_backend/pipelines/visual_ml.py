# This Python module implements the ML logic for the Visual Impairment Pipeline.
# It receives image crops of unlabelled DOM elements and queries the Gemini Vision model.
# It returns generated ARIA labels, alt-text descriptions, and contrast recommendations
# back to the visual-wire.ts file in the Chrome Extension.
