# This Python module handles saving and fetching usage telemetry for the Dashboard.
# It writes interaction logs (e.g., 'hitbox expanded', 'text simplified') to the database.
# It serves the historical aggregation data needed for the Dashboard React UI.
